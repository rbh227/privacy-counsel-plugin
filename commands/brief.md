---
description: Compress a matter or document set into what actually matters
argument-hint: "[files / context to brief on]"
disable-model-invocation: true
---

Brief per the partner-briefing skill: $ARGUMENTS

Output exactly: Bottom line / What matters (3–5 bullets, materiality
invariant applies) / Decision needed (only if real) / Next move (1–3
actions).
